version: "3.8"
services:
  telegram-lang-bot:
    image: telegram-lang-bot:latest
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    ports:
      - "${MONITOR_PORT:-8080}:${MONITOR_PORT:-8080}"
    env_file: .env
